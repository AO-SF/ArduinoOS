WATCH(1) - User Commands

NAME
      watch - run a program repeatedly with a delay

SYNOPSIS
      watch interval program [programarg]

DESCRIPTION
      Runs the given program (with the given arg, if present) immediately, and again every interval seconds thereafter.
