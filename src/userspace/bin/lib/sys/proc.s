const PidMax 16
const ArgLenMax 64
const EnvVarPathMax 128
const MaxFds 9 ; maximum amount of open files a single process can have (although local fd=0 is used for invalid so there are only 8 available for us)
const ArgMax 255
